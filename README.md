🚀 Гайд по установке NixBliss на новое устройство

Короткий и понятный README, который можно закинуть в репозиторий:
1. Подготовка

Установи NixOS (минимальный образ) и убедись, что Git установлен:
Bash

nix-shell -p git

2. Клонирование
Bash

git clone https://github.com/Zidrax/NixBliss.git ~/dotfiles
cd ~/dotfiles

3. Генерация конфига железа

Сгенерируй файл, специфичный для твоего текущего ПК/ноутбука:
Bash

sudo nixos-generate-config --show-hardware-config > hardware-configuration.nix

    Важно: Поскольку ты используешь Flakes, Гит должен «знать» о новом файле, иначе сборка его не увидит:
    Bash

    git add hardware-configuration.nix

4. Применение конфига

Запусти сборку системы (замени my-hostname на имя хоста, прописанное в flake.nix в секции nixosConfigurations):
Bash

sudo nixos-rebuild switch --flake .#default

