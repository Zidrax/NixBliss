# ✨ NixBliss

Конфигурация NixOS на базе Hyprland. Оптимизирована под разработку, гейминг и работу с ИИ.

# 🚀 Установка NixBliss на новое устройство

1. Подготовка
Временно. устанавливаем git в систему:
```Bash
nix-shell -p git
```
2. Клонирование:
```Bash
git clone https://github.com/Zidrax/NixBliss.git ~/dotfiles
cd ~/dotfiles
```

Либо клонирование по ssh:
```bash
git clone git@github.com:Zidrax/NixBliss.git ~/dotfiles
cd ~/dotfiles
```

3. Генерация конфига железа

Сгенерируй файл, специфичный для твоего текущего ПК/ноутбука:
```Bash
sudo nixos-generate-config --show-hardware-config > hardware-configuration.nix
```

4. В flake.nix можно изменить юзера на своего

5. Применение конфига

Запусти сборку системы:
```bash
sudo nixos-rebuild switch --flake .#laptop
```

# ⌨️ Горячие клавиши (Hotkeys)

## Основные (Hyprland):

Super + Enter — Открыть терминал (Kitty) 

Super + Q — Закрыть активное окно 

Super + D — Меню запуска (Rofi) 

Super + E — Файловый менеджер (Nautilus) 

Super + V — Переключить плавающий режим 

Super + L — Заблокировать экран (Hyprlock) 

Super + M — Меню выключения 

Super + N — Центр уведомлений (SwayNC) 

## Инструменты и Утилиты:

Super + C — История буфера обмена 

Super + Shift + C — Калькулятор 

Super + . (Period) — Выбор Emoji 

Super + I — Скрипт настройки чувствительности 

Super + Shift + V — Просмотр изображения из буфера 

## Скриншоты:

Print — Выделенная область в буфер 

Shift + Print — Весь экран в буфер 

Ctrl + Print — Выделенная область в файл 

Навигация:

Super + 1-0 — Переход на воркспейс 

Super + Shift + 1-0 — Переместить окно на воркспейс 

Super + S — Специальный воркспейс (скретчпад) 

## Внутри Neovim:

Ctrl + N — Открыть/переключить фокус на дерево файлов (NvimTree)
